int[] numbers = { 1, 2, 3, 4, 5 };

// Arrays have a property "Length" which it says "I have this many items"
// Can also use a foreach loop to iterate over each item. (See !code-loops for examples)
for(int i = 0; i < numbers.Length; i++)
    Console.WriteLine(numbers[i]);