numbers = [1,2,3,4,5]

# can do a for each loop on the array
for x in numbers:
    print(x)

# can iterate over each item
# len(arrayVariable) -- gets length of array
for x in range(0, len(numbers)):
    # below is an example of us getting the item at position 'x'
    print(numbers[x])